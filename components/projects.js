document.addEventListener('DOMContentLoaded', function() {
    const projects = `
        <main>
            <section id="projects">
                <h2>Our Projects</h2>
                <p>We have worked on a variety of projects ranging from enterprise-level applications to innovative startups.</p>
            </section>
        </main>
    `;
    document.body.insertAdjacentHTML('beforeend', projects);
});
